apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: vault-backend
  namespace: default
spec:
  provider:
    vault:
      server: "http://vault.vault:8200"
      path: "kvv2"
      version: "v2"
      auth:
        tokenSecretRef:
          name: "vault-token"
          key: "token"
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: mimir-secret
  namespace: mimir
spec:
  refreshInterval: "15s"
  secretStoreRef:
    name: vault-backend
    kind: SecretStore
  target:
    name: mimir-secret
  data:
  - secretKey: zip
    remoteRef:
      key: secret
      property: zip
  - secretKey: foo
    remoteRef:
      key: secret
      property: bar
