apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: openbao-backend
  namespace: loki
spec:
  provider:
    vault:
      server: "http://openbao.openbao:8200"
      path: "loki"
      version: "v2"
      auth:
        tokenSecretRef:
          name: "openbao-token"
          key: "token"
          namespace: loki
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: loki-secret
  namespace: loki
spec:
  refreshInterval: "15s"
  secretStoreRef:
    name: openbao-backend
    kind: SecretStore
  target:
    name: loki-secret
  data:
  - secretKey: .htpasswd
    remoteRef:
      key: secret
      property: htpasswd
