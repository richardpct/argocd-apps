apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: vault-backend
  namespace: ceph-csi
spec:
  provider:
    vault:
      server: "http://vault.vault:8200"
      path: "ceph-csi"
      version: "v2"
      auth:
        tokenSecretRef:
          name: "vault-token"
          key: "token"
          namespace: ceph-csi
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: ceph-csi-secret
  namespace: ceph-csi
spec:
  refreshInterval: "15s"
  secretStoreRef:
    name: vault-backend
    kind: SecretStore
  target:
    name: ceph-csi-secret
  data:
  - secretKey: userID
    remoteRef:
      key: secret
      property: userID
  - secretKey: userKey
    remoteRef:
      key: secret
      property: userKey
